import { CvslistsortPipe } from './cvslistsort.pipe';

describe('CvslistsortPipe', () => {
  it('create an instance', () => {
    const pipe = new CvslistsortPipe();
    expect(pipe).toBeTruthy();
  });
});
